#ifndef _ATOM_{{atom_num}}_H__{{monitor_name}}
#define _ATOM_{{atom_num}}_H__{{monitor_name}}


#include "atom-identifier.h"
#include "regular-atom-monitor.h"

{{cg.namespace_start()}}

/* Formula: {{formula}} */
class AtomMonitor{{atom_num}} : public RegularAtomMonitor {
public:
  AtomMonitor{{atom_num}}(const Instance &instance);
  AtomMonitor{{atom_num}}(const Instance &instance, FormulaEvaluationState st);

  Verdict step(unsigned num = 0) { return {{verdict}}; }
};

{{cg.namespace_end()}}

#endif
